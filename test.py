
# index = [{'age':"'age'"}, "'sex'", "'cp'", "'trestbps'", "'chol'", "'fbs'", "'restecg'", "'thalach'",
#        "'exang'", "'oldpeak'", "'slope'", "'ca'", "'thal'"]
index = [{'age':"'age'"}]
for list in range(len(index)):
    print(list)
    print(index[list])
    print(index[list]['age'])